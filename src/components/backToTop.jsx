import styled from "styled-components";

const StyledBtn = styled.button``;

const BackToTop = () => {
  return (
    <StyledBtn>
      <i className="fa-solid fa-arrow-up"> Back to top</i>
    </StyledBtn>
  );
};

export default BackToTop;
