import styled from "styled-components";

const StyledItems = styled.section`
  display: flex;
  flex-wrap: wrap;
  padding: 20px;
  gap: 20px;
  h1.title {
    width: 100%;
  }
`;

export default StyledItems;
